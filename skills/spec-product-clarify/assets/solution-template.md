---
title: 产品需求方案决策（Solutionate）
status: draft
---

> 目的：把“推荐决策 + 备选方案对比 + 决策依据 + 验证清单”落到一份可评审文档里，作为后续 `prd.md` 与 `prototype.md` 的**唯一决策入口**。
>
> 原则：结论先行；只保留支撑决策的最小信息；不写“待确认问题”清单——所有不确定性统一进入“验证清单”（Owner/截止/动作明确）。
>
> 简单需求快速通道：若跳过 `requirements/prd.md`，在文末补齐 “Mini-PRD”。

## 0. 基本信息

- 需求标识（分支 / ID）：【必填】
- 作者 / 参与评审：【必填】
- 状态：draft / reviewing / approved【必填】
- 最后更新：YYYY-MM-DD【必填】
- 关联链接（工单、讨论、资料）：【可选】

## 1. 结论摘要（先给结论，3–7 行）

- 一句话目标（要解决什么）：【必填】
- 本次 In / Out 的边界（一句话）：【必填】
- 推荐方案（命名 + 1 句机制概述）：【必填】
- 优先验证点（引用 “V-xxx”，1–3 个）：【必填】

## 2. 推荐方案（必须 1 个）

- 方案名：【必填】
- 主流程 / 关键机制（3–6 步/条）：【必填】
- 关键边界/取舍（≥ 3 条）：【必填】
- 为什么选它（1–3 条，必须可追溯到证据/约束或验证清单）：【必填】

## 3. 备选方案（2–3 个，差异明显）

> 复制下面小节 2–3 次即可；每个备选只保留“何时会选 / 不选原因”的关键差异（1–2 条）。

### 3.X 备选方案：【命名】

- 核心机制（1–2 句）：【必填】
- 主流程（3–6 步）：【必填】
- 边界与取舍：【必填，1–3 条】
- 适用前提（何时会选它）：【必填，1–3 条】
- 不选原因（1–2 句关键差异）：【必填】

## 4. 决策依据（证据入口清单）

- `{FEATURE_DIR}/requirements/raw.md` 引用点位（段落/条目/链接）：【必填】
- 数据/约束来源（如有）：【可选】
- 若缺少证据：必须在“验证清单”中补齐对应验证项：【必填】

## 5. 验证清单（V-xxx，可执行）

> 每条都要能落到：风险/假设 → 方法 → 成功/失败信号 → Owner → 截止 → 触发动作（成立/不成立怎么做）。Owner/截止至少写到“角色/负责人 + 相对期限”，禁止 TBD。

- V-001（示例）导出性能目标未定
  - 风险/假设：MVP 若采用同步导出，可能超时或影响线上性能
  - 方法：在预发按 1 万/10 万/100 万行压测；记录 p95 耗时与失败率
  - 成功/失败信号：p95 < 30s 且失败率 < 0.1% 为成立；否则不成立
  - Owner：DEV
  - 截止：评审后 3 天
  - 触发动作：不成立则切换为异步导出（任务队列 + 导出中心）

- V-xxx（复制本条追加）
  - 风险/假设：
  - 方法：
  - 成功/失败信号：
  - Owner：
  - 截止：
  - 触发动作：

## 6. 迭代记录（追加，不覆盖）

- YYYY-MM-DD：本轮变更摘要（相对上一轮改了什么、为什么）【必填】
- YYYY-MM-DD：…

## 7. Impact Analysis（需求影响分析）

> 目的：把项目知识库消费成后续 D2/I1 可直接引用的约束输入（受影响模块、不变量、跨模块影响、缺口）。

### 7.1 受影响模块

| 模块 | 影响类型 | 关键不变量 | stale? |
|------|----------|-----------|--------|
| <module> | 新增能力/修改契约/读取数据/... | <摘要 + 来源锚点> | yes/no |

### 7.2 需遵守的不变量

- [不变量 1]（来源：`.aisdlc/project/components/<module>.md#...`）
- [不变量 2]（来源：`.aisdlc/project/components/<module>.md#...`）

### 7.3 跨模块影响

- 改了 A → 需关注 B（原因：<调用关系/数据依赖>）

### 7.4 Context Gaps

- `CONTEXT GAP`：<缺失的必读文件/缺失的模块 TL;DR/模块 stale> → 建议动作：<例如先做 Delta Discover / 补权威入口>

## 8. Mini-PRD（仅在跳过 `requirements/prd.md` 时必填）

- **MVP 范围**：In / Out（精确到行为/规则）
- **验收标准（AC）**：3–10 条即可；逐条可测试、可验证
- **交互变化结论**：无 / 有但简单（1–3 句说明原因）；若存在新增/变更交互且不够明确，建议改走 PRD/原型路径
- **影响面**：涉及页面/入口/接口/权限点（给出可定位的名称/路径/契约入口）

