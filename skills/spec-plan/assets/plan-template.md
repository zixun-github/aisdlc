---
title: I1 Implementation Plan（模板 / SSOT）
status: draft
---

目的：把 `requirements/*` 与 `design/*` 转为**可直接执行**的实现计划，并将其作为唯一执行清单与状态 SSOT（checkbox + 步骤 + 验证 + 提交点 + 审计信息）。  
落盘位置：`{FEATURE_DIR}/implementation/plan.md`

> 约束：
> - 必须先 `spec-context` 拿到 `FEATURE_DIR=...`，失败即停止
> - 不写“待确认问题清单”；所有不确定性只写在 `## NEEDS CLARIFICATION`（未消除前不得进入 I2）

# [需求名] 实现计划（SSOT）

> **必需技能：** `spec-implementation-execute`（按批次执行本计划）
> **上下文门禁：** 必须先用 `spec-context` 定位 `{FEATURE_DIR}`，失败即停止

**目标：** [一句话描述要交付什么]  
**范围：** In / Out  
**架构：** [2–3 句方法说明 + 关键约束]  
**验收口径：** [引用 requirements/solution.md 或 requirements/prd.md 的 AC/验收点]  

---

## TL;DR（3–7 行）

- 一句话目标：
- In/Out：
- 关键路径（1–3 条）：
- 最大风险与优先验证点（引用下文编号）：

---

## 范围与边界（In / Out）

- **In**：
- **Out**：
- **不变量/关键约束**：
- **影响面**（模块/接口/权限/数据口径/运维）：

---

## 里程碑与节奏

- M0（MVP）：
- M1（可选）：

> 每个里程碑必须可验收（产物/验证标准明确），并能映射到任务清单中的任务集合。

---

## 依赖与资源

- 环境/权限：
- 外部系统/团队：
- 数据/样本：
- 发布/变更窗口（如适用）：

---

## 风险与验证（可执行）

| # | 风险/假设 | 验证方式 | 成功信号 | 失败信号 | Owner | 截止 | 下一步动作 |
|---|---|---|---|---|---|---|---|
| R1 |  |  |  |  |  |  |  |

---

## 验收口径（可追溯）

- 追溯：`requirements/solution.md`（段落/条目）：
- 追溯：`requirements/prd.md`（如适用；AC-xxx）：
- 关键验收点（摘要，不要重写整份 PRD）：

---

## NEEDS CLARIFICATION（未消除前不得进入 I2）

> 把不确定性写成“缺什么 → 如何取证/验证 → 期望看到什么信号 → 下一步动作”，不要写成悬空问题。

- C1：
  - 缺什么：
  - 取证/验证方式：
  - 成功/失败信号：
  - 下一步动作：

---

## 任务清单（SSOT）

> 这是唯一的执行清单与状态来源：用 `- [ ] / - [x]` 标记完成；执行中把 `commit/pr/changed_files` 与关键验证结果回写到对应任务。
> 命令默认面向 PowerShell；同一行多命令请用 `;` 分隔（不要用 `&&`）。

### Task T1: [任务标题]

- [ ] **状态**：未开始 / 进行中 / 完成 / 阻塞（阻塞必须写明取证路径）

**文件：**
- 创建：
- 修改：
- 测试：

**验收点：**
- 

**步骤 1：写失败测试（如适用）**
- Run:
- Expected: FAIL（关键失败信号）

**步骤 2：写最少实现**
- 修改点：

**步骤 3：运行验证**
- Run:
- Expected: PASS（关键通过信号）

**步骤 4：提交（频繁提交；commit message 必须中文）**
- Commit message:
- 审计信息：`commit=<TBD>`、`pr=<TBD>`、`changed_files=<TBD>`

---

## Merge-back 待办清单（仅记录，不在本阶段执行）

> 若实现中产生“需要晋升到 project/”的变更（ADR/契约/NFR/运维等），在这里记录：要晋升什么、证据入口在哪里、谁来做。

- MB-001：

